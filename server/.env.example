# 复制为 .env 后按需修改
DATABASE_URL=postgres://shuf:shuf@localhost:5432/shuf
PORT=3001
# 会话有效期（天），滚动续期
SESSION_TTL_DAYS=90
# 会话 Cookie 名
COOKIE_NAME=sf_sid
# 生产（https）时设为 production 以启用 Secure Cookie
NODE_ENV=development
